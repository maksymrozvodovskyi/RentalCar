export const customSelectBrandStyles = {
  control: (provided) => ({
    ...provided,
    borderRadius: "12px",
    width: "204px",
    height: "44px",
    background: "#f7f7f7",
    fontFamily: "Manrope, sans-serif",
    fontWeight: 500,
    fontSize: "16px",
    lineHeight: 1.25,
    color: "#101828",
    boxShadow: "none",
    cursor: "pointer",
    border: 0,
  }),
  option: (provided, state) => ({
    ...provided,
    fontFamily: "Manrope, sans-serif",
    fontWeight: 500,
    fontSize: "16px",
    lineHeight: 1.25,
    color: state.isSelected ? "#101828" : "#8d929a",
    backgroundColor: "#fff",
    cursor: "pointer",
    ":active": {
      backgroundColor: "#fff",
    },
    padding: "0 18px",
    marginBottom: "12px",
    ":last-child": {
      marginBottom: 0,
    },
  }),
  menu: (provided) => ({
    ...provided,
    borderRadius: "12px",
    width: "204px",
    border: "1px solid #f7f7f7",
    boxShadow: "0 4px 36px 0 rgba(0, 0, 0, 0.02)",
  }),
  menuList: (provider) => ({
    ...provider,
    padding: "14px 0",
  }),
  singleValue: (provided) => ({
    ...provided,
    fontFamily: "Manrope, sans-serif",
    fontWeight: 500,
    fontSize: "16px",
    lineHeight: 1.25,
    color: "#101828",
  }),
  placeholder: (provided) => ({
    ...provided,
    fontFamily: "Manrope, sans-serif",
    fontWeight: 500,
    fontSize: "16px",
    lineHeight: 1.25,
    color: "#101828",
  }),
};
